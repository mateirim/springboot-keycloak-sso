apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: springboot-app
  namespace: apps
spec:
  serviceName: springboot-app
  replicas: 1
  selector:
    matchLabels:
      app: springboot-app
  template:
    metadata:
      labels:
        app: springboot-app
    spec:
      imagePullSecrets:
        - name: registry
      containers:
        - name: springboot-app
          image: registry.example.com/springboot-keycloak-sso:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 8080
          env:
            - name: KEYCLOAK_CLIENT_ID
              value: springboot-app
            # Auth realm — where springboot-app client lives and users log in
            - name: KEYCLOAK_ISSUER_URI
              valueFrom:
                secretKeyRef:
                  name: springboot-app-secrets
                  key: KEYCLOAK_ISSUER_URI
            # Keycloak Client Secret
            - name: KEYCLOAK_CLIENT_SECRET
              valueFrom:
                secretKeyRef:
                  name: springboot-app-secrets
                  key: KEYCLOAK_CLIENT_SECRET
            - name: CORS_ALLOWED_ORIGINS
              valueFrom:
                secretKeyRef:
                  name: springboot-app-secrets
                  key: CORS_ALLOWED_ORIGINS
            - name: SERVER_FORWARD_HEADERS_STRATEGY
              value: native
            - name: SERVER_TOMCAT_REMOTE_IP_HEADER
              value: x-forwarded-for
            - name: SERVER_TOMCAT_PROTOCOL_HEADER
              value: x-forwarded-proto
            - name: SERVER_TOMCAT_USE_RELATIVE_REDIRECTS
              value: "false"
          livenessProbe:
            httpGet:
              path: /api/health
              port: 8080
            initialDelaySeconds: 30
            periodSeconds: 20
            failureThreshold: 3
          readinessProbe:
            exec:
              command:
                - sh
                - -c
                - 'true'
            initialDelaySeconds: 15
            periodSeconds: 10
          resources:
            requests:
              memory: "512Mi"
              cpu: "100m"
            limits:
              memory: "2Gi"
              cpu: "1000m"
      terminationGracePeriodSeconds: 60
